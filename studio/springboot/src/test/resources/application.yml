spring:
  application:
    name: langgraph4j::studio::springboot
  mvc:
    cors:
      allowed-origins: "http://localhost:1234"
      allowed-methods: GET,POST,PUT,DELETE,OPTIONS
      allowed-headers: "*"
      allow-credentials: true
      max-age: 1800